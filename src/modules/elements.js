const scoreTable = document.querySelector('.score-table');
const form = document.querySelector('.form');
const refresh = document.querySelector('.refresh-button');

export { scoreTable, refresh, form };
